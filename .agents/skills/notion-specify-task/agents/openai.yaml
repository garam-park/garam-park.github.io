interface:
  display_name: "Notion 할 일 명세 초안"
  short_description: "분석 결과를 Notion 관리 명세 초안으로 저장하고 자동 리뷰 준비"
  default_prompt: "Use $notion-specify-task with 작업 ID: <ID> to save the matching analysis as a review-ready Notion specification draft."

dependencies:
  tools:
    - type: "mcp"
      value: "notion"
      description: "Project-local Notion MCP for verified task-body updates"
