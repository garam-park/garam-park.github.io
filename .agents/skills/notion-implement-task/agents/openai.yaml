interface:
  display_name: "Notion 할 일 구현"
  short_description: "확정 명세와 포스트 검색 Gate를 작업 브랜치에서 구현하고 검증"
  default_prompt: "Use $notion-implement-task with 작업 ID: <ID> to implement and verify that approved StoryG Blog task specification."

dependencies:
  tools:
    - type: "mcp"
      value: "notion"
      description: "Project-local Notion MCP for task verification, status updates, and start comments"
