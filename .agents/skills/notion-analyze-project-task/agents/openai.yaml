interface:
  display_name: "Notion 프로젝트 할 일 분석"
  short_description: "StoryG Blog 할 일을 읽기 전용으로 선택하고 분석"
  default_prompt: "Use $notion-analyze-project-task to select and analyze the next StoryG Blog task without making changes."

dependencies:
  tools:
    - type: "mcp"
      value: "notion"
      description: "Project-local read-only access to the configured Notion workspace"
