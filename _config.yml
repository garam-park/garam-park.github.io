# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Story's G Blog
email: garam-park@naver.com
description: "개발자가 AI와 인프라로 자기 사업을 직접 굴리는 실측기. K8s·DevOps·AI 운영부터 실서비스까지."
tagline: "AI와 인프라로 내 사업을 굴리는 개발자의 실측 기록"
author: Garam Park
baseurl: "" # the subpath of your site, e.g. /blog
# SEO 원본 도메인. GitHub Pages도 같은 정적 파일을 제공하지만, canonical·OG·sitemap은
# 검색용 원본인 blog.storyg.co를 가리켜 중복 콘텐츠 신호를 피한다.
url: "https://blog.storyg.co"

# 댓글 (giscus) — GitHub Discussions 기반. https://giscus.app 에서 repo·category 선택 후 값 채우기.
# 네 값이 모두 채워지면 포스트 하단에 자동 노출되고, 비어 있으면 댓글 영역은 렌더링되지 않는다.
giscus:
  repo: ""         # 예: garam-park/garam-park.github.io
  repo_id: ""      # giscus.app 에서 발급되는 repo ID
  category: ""     # 예: Announcements
  category_id: ""  # giscus.app 에서 발급되는 category ID
# twitter_username: jekyllrb
# github_username:  jekyll

# Build settings
markdown: kramdown
theme: minima

plugins: [jekyll-feed, jekyll-paginate]
paginate: 5
paginate_path: "page:num"

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
exclude: 
  - package.json
  - yarn.lock
  - node_modules/
  - .vscode/
  - .DS_Store
  - tsconfig.json
  - .gitignore
  - Dockerfile
  - .dockerignore
  - docker-compose.yml
  - docker-compose.dev.yml
  - nginx.conf
  - readme.md
  - readme.old.md
  - scripts/